/* Select all columns from emp table.  */
SELECT * FROM EMP;

/* Select the JOB column from EMP table.  */
SELECT JOB FROM EMP;

/* Select the JOB and ENAME columns from EMP table.  */
SELECT JOB, ENAME FROM EMP;

/* Select all columns from dept table.  */
SELECT * FROM DEPT;

/* Select DNAME and LOC columns from DEPT table.  */
SELECT DNAME, LOC FROM DEPT;

/* Select all unique JOB values (no duplicates) from EMP table.  */
SELECT DISTINCT JOB FROM EMP;